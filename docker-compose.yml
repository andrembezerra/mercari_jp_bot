
services:
  mercari-bot:
    build: .
    image: andre/mercari-bot:buyee-site
    container_name: mercari-bot
    restart: unless-stopped
    env_file:
      - key.env
    volumes:
      - .:/app
    working_dir: /app
    command: python run_bot.py
